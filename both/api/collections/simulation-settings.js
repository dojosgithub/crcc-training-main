// if(Meteor.isServer) {
    
    import { Meteor } from 'meteor/meteor'
    import { Mongo } from 'meteor/mongo'

    export const SimulationSettings = new Mongo.Collection("simulation_settings");
   
   // }
   